using System;

namespace MorningAssignment;

internal class CountNumbers
{
    //public static int CountAllNumbers(int input)
    //{
    //    int positiveCount = 0;
    //    int negativeCount = 0;
    //    int integerCount = 0;

    //    string[] numbers = input.Split(' ');

    //    foreach (string numStr in numbers)
    //    {
    //        if (int.TryParse(numStr, out int num))
    //        {
    //            if (num > 0)
    //                positiveCount++;
    //            else if (num < 0)
    //                negativeCount++;

    //            integerCount++;
    //        }
    //    }       
    //}
}
